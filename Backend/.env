# Async SQLAlchemy URL (asyncmy)
DATABASE_URL=mysql+asyncmy://root:ADserver@54321@localhost:3306/nutriwise

# Optional: auto-apply sql/*.sql on API startup (dev convenience)
APPLY_SQL_ON_STARTUP=false
APP_ENV=dev
